.DS_Store
.idea
/node_modules
yarn.lock

# Serena MCP project data (cache, memories, project config)
.serena/

# Claude MCP project data (cache, memories, project config)
.claude/settings.local.json

# Oh My Claude (OmC) project data — runtime state/cache; skills are the exception
!.omc/
.omc/*
!.omc/skills/
!.omc/skills/**

# Legacy OMX runtime (if present)
.omx/

# Backups
*.bak

# Logs
.logs/

# Private, fork-only icon components (never pushed)
lib/components/icons/private/

# Local MCP config — contains secrets, never pushed (public fork)
.mcp.json
